import AddTaskForm from '.';

export default {
  title: 'component/organisms/AddTaskForm',
  component: AddTaskForm,
};

const Template = () => <AddTaskForm />;

export const Default = Template.bind({});
